using System;
using BackEnd.Objects;
using BackEnd;

namespace BackEnd.Method
{
    public class WhoWin
    {

        public static String SwitchP1(Player Player1,Player Player2)
        {

            /*
            String Win = "";

            if(Player1.Option == Player2.Option)
            {
                Win = Strings.Draw;
            }
            else
            {
                switch (Player1.Option)
                {
                    case "STONE":
                        Win = CasePlayer2.WinnerStone(Player2.Option,Player1.Name,Player2.Name);
                        break;
                    case "PAPER":
                        Win = CasePlayer2.WinnerPaper(Player2.Option, Player1.Name, Player2.Name);
                        break;
                    case "SCISSORS":
                        Win = CasePlayer2.WinnerScissors(Player2.Option, Player1.Name, Player2.Name);
                        break;
                    case "SPOKE":
                        Win = CasePlayer2.WinnerSpoke(Player2.Option, Player1.Name, Player2.Name);
                        break;
                    case "LIZARD":
                        Win = CasePlayer2.WinnerLizard(Player2.Option, Player1.Name, Player2.Name);
                        break;
                }
            }
            */



            return Win;
        }
    }
}
